---
description: BA Assistant — generate or refresh the interactive project canvas
---
Run the BA Assistant /canvas command (skills/ba-assistant/sub-skills/ba-project-canvas): read the capability file per its load map, then produce BOTH outputs (8-tab .canvas.tsx + status-snapshot.html) from current tracker + Jira state.
