---
description: BA Assistant — full status: chat + canvas + HTML (triple output)
---
Run the BA Assistant /status command per rules/ba-profile.mdc and skills/ba-assistant: sync Jira first, then produce all three outputs (chat status with workstream grid and tracker, canvas refresh, status-snapshot.html). Never produce the chat status alone. If no BA initiative is active in this workspace, say so — do not invent one.
