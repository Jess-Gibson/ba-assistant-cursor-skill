---
description: BA Assistant — end-of-session closeout: validate, promote, checkpoint
---
Run the BA Assistant /wrap command per sync-gates: action runthrough on every open action, then /validate-state, promote unpromoted SESSION-CONTEXT items to the tracker, refresh the workboard, suggest a new chat.
