# Initiatives

Default root for BA Assistant initiative folders when
`BA_INITIATIVES_ROOT` is unset.

After install, the live folder is created at `~/.cursor/initiatives/`.

Start work with: `Start a new initiative called [name]`
