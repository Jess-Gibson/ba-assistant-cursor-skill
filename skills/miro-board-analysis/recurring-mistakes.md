# Recurring Mistakes Log

**This is a living document.** When a new mistake is identified during board creation or verification, append a row to the table below. Include the date, what went wrong, why, and the corrective rule with a cross-reference to the canonical location of the fix.

---

| Date | Mistake | Root cause | Corrective rule |
|---|---|---|---|
| May 2026 | Used `doc_create` for debrief content | Wrong tool choice — docs don't match board visual system | Use Content Panel Pattern (header + grey box + text) — never `doc_create` for content that should match board styling. See `design-system.md` Content Panel Pattern. |
| May 2026 | Used `user-miro-desktop` `create_shape`/`create_text` — styling silently dropped | These tools have limited style params | Always use `layout_create` DSL from `plugin-miro-miro` / `user-miro-mcp` for styled content. See `SKILL.md` MCP Servers. |
| May 2026 | Tables placed without headers — unreadable at zoom-out | Assumed table content was self-explanatory | Every table MUST have a coloured header shape above it. See `templates/debrief-board.md` Pattern 11. |
| May 2026 | Headers misaligned with table content (different x center) | Didn't calculate table rendered center | Header center x must match table center x. See `design-system.md` Table Section DSL snippet. |
| May 2026 | Grey boxes too wide for text content | Used fixed width from template | Size grey box width to match text density — 800-1000px for narrative, wider for tables. See `design-system.md` Width Discipline. |
| May 2026 | Reading order didn't match document structure | Placed sections where they fit rather than in logical order | Left-to-right order must match document section order. See `design-system.md` Section Spacing & Positioning. |
| May 2026 | Omitted document sections (Decisions Deferred, Knowns, Attendance) | Tried to abbreviate | Include ALL sections from source document. See `templates/analysis-board.md` Content Completeness Checklist. |
| May 2026 | Text vertically centered in grey box instead of top-aligned | Set `text_y = grey_box_y` — which places text top at the grey box's vertical midpoint (because SHAPE y is center-anchored but TEXT y is top-edge-anchored) | Formula: `text_y = grey_box_y - grey_box_h/2 + 15` (i.e. grey box top edge + 15px margin). TEXT y is top-edge-anchored, SHAPE y is center-anchored — never confuse them. See `design-system.md` CRITICAL POSITIONING RULES. |
| Jun 2026 | Text flush against top of grey box — no visible margin, looked cramped | Agent set `text_y = grey_box_top` (0px offset) — technically "top aligned" but no breathing room | Always add 15px offset: `text_y = grey_box_top + 15`. The 15px is empirically calibrated — 0px looks cramped, 30px+ creates too much whitespace. See `design-system.md` CRITICAL POSITIONING RULES. |
| Jun 2026 | Inconsistent text alignment across grey boxes in the same row | Agent calculated text positions using different methods for different boxes (some center-anchored, some top-anchored, some estimated) | **All text positions must use the same formula:** `text_y = grey_box_y - grey_box_h/2 + 15`. Verify consistency: every text element in a row should have this same relationship to its grey box. See `design-system.md` CRITICAL POSITIONING RULES check 5. |
| May 2026 | Used `round_rectangle` for grey backdrops — default radius (~50px) looks too round | Miro DSL cannot set custom corner radius. `round_rectangle` defaults to ~50px which is too round for grey backdrop boxes | **Always use `rectangle` for grey backdrops.** The user manually adjusts to 20px radius. Headers keep `round_rectangle` (smaller shapes, default radius acceptable). See `design-system.md` Grey Card Shape Type. |
| May 2026 | Table headers placed at same x as table — but tables anchor at left edge, not centre | Miro tables report `x` as their left edge, while shapes use `x` as centre | For multi-column table layouts, offset header x: `header_x ≈ table_x + (estimated_table_width / 2)`. For ~6 col tables this is ~900px offset. Always `layout_read` after user fixes to learn actual positions. See `templates/debrief-board.md` Table Header Alignment. |
| May 2026 | Tables stacked vertically — wasted horizontal space and hard to scan | Defaulted to vertical stacking when multiple tables present | Use a multi-column grid. Pair shorter tables within a column. Keep frame tight. See `templates/debrief-board.md` Pattern 12. |
| May 2026 | Table columns too wide for content (e.g. select columns rendering very wide) | Miro API doesn't support column width control | Accept that table column widths need manual adjustment. Flag this to the user as expected post-creation work. |
| Jun 2026 | `plugin-miro-miro` returned "Unauthorized" then disappeared from server list entirely | Miro OAuth session expired. Re-auth didn't restore the plugin server — only `user-miro-desktop` (basic 9-tool set) remained available. | Check server availability BEFORE planning tool usage. If only `user-miro-desktop` available, switch to one-call-per-item strategy. Don't plan DSL-based layout_create calls that will fail. Ask user to re-auth, but have fallback ready. See `SKILL.md` MCP Servers and Fallback Strategy. |
| Jun 2026 | Analysis board Frame 1 was "terrible" — tiny text, no grey boxes, cramped layout, unusable | Agent skipped the planning phase and went straight to DSL generation. Used size=13-16 for body text (should be 30-36), size=24 for headers (should be 64), frame was 3000px wide (should be 5000+ minimum), no grey backdrop boxes at all. | **ALWAYS follow the 6-pass Board Construction Algorithm.** Never create layout DSL without first completing passes 1-4. Use the Dimensional Reference Table for all hard values. Minimum thresholds: frame >= 5000px wide, headers >= size 48, body >= size 26, every text block needs a grey backdrop box. See `algorithm.md` and `design-system.md` Minimum Thresholds. |
| Jun 2026 | Grey boxes too small — text overflowed bottom edge. Problem Statement, Scope, and Key People all undersized by 36-166px. | Agent estimated ~42px per line but actual Miro rendering at size=30 uses ~46px. Bold text and HTML entities consume more horizontal space, causing more wraps than a simple character count suggests. Top padding was also 50px (should be 15px). | **Three fixes codified:** (1) Use 46px/line not 42px. (2) Count line wraps: at w=786, assume ~42 chars/rendered-line. A 100-char paragraph = 3 rendered lines = 138px, not 1 line = 46px. (3) Apply 15% safety margin on estimated text height before calculating grey box height: `grey_box_h = (text_h × 1.15) + 65`. (4) Top padding = 15px (empirically calibrated from user-approved layouts). See `algorithm.md` Pass 3. |
| Jun 2026 | Columns not centered in frame — left margin 100px, right margin 560px | Agent placed first column at x=100 without calculating frame center. Content center ≠ frame center. | **All content must be centered.** Calculate `content_span` and ensure left margin = right margin = `(frame_w - content_span) / 2`. Verify in Pass 6. See `algorithm.md` Pass 4 and `verification-checklist.md`. |
| Jun 2026 | Bottom sections full-width (w=2940) when content didn't warrant it | Agent defaulted to spanning the full column span for any below-columns section | **Never use full-width for singular text boxes.** Full-width only for diagrams, tables, images, flows. Text sections should be column-width (812-900px). N content sections = N horizontal columns. See `design-system.md` Width Discipline. |
| Jun 2026 | Duplicate title bars — frame title rendered as visible bar AND agent created a shape title with same text | Agent named the frame "Solution Options (23 Jun Reframe)" and also created a purple shape with the same text. Miro renders both. | **Frame name ≠ title shape text.** Frame name = short ID (e.g. "3. Solution Options"). Title shape = descriptive text. See `verification-checklist.md` check 8. |
| Jun 2026 | Skipped Pass 6 verification — user caught sizing issues agent missed | Agent declared frame "complete" without running `layout_read` verification | **Pass 6 is mandatory. Never skip it.** Run `layout_read` after every `layout_create` and check all items before reporting to user. See `verification-checklist.md`. |
| Jun 2026 | Section headers used frame-title sizing (h=82, size=64) — sections looked like separate frames | No header hierarchy defined | **Two header tiers:** frame title (h=82, size=64) and section headers (h=70, size=42). Sub-labels (h=36, size=22). See `design-system.md` Header Hierarchy. |
| Jun 2026 | Missing RAID items, dependencies, and decisions from analysis board — content incomplete | Agent built from summary/memory instead of reading source documents | **Always read ALL source files** (initiative-tracker.md, solution outputs, debriefs) before building. Don't summarise. Include all RAID items, decisions with IDs, and dependencies. Filter by current decision state. See `templates/analysis-board.md` Content Completeness Checklist. |
| Jun 2026 | Agent skipped entire 6-pass algorithm despite having just refactored the skill into modular files. Produced a worse board (V2) than the previous attempt (Frame 5) built with the old monolithic skill. | **Author bias:** Having written all the modular files in the same conversation, the agent treated "I know what's in these files" as equivalent to "I am following these files." It never re-read SKILL.md as an entry point, never loaded the analysis template, never ran passes 1-4, never produced a coordinate manifest, and never ran verification. The plan review gate — the single most important safeguard — was bypassed entirely. | **Always re-enter through SKILL.md.** Even if you authored the files, even if you "know" the content, even if the user said "just build it." Read the orchestrator → follow the routing table → read the template → run passes 1-4 → produce a plan → get approval → THEN build. See `SKILL.md` STOP Gates. |
| Jun 2026 | Grey boxes normalised to tallest in row — massive empty grey space. RAID boxes all set to 1760px; Assumptions section had ~576px of content, creating 1184px of empty grey. Bottom row boxes all 1160px; Decisions had ~504px content, creating 656px of empty grey. | Agent normalised grey box heights within each row so bottom edges would align. This "fixed" visual unevenness but created the exact "too much grey space" problem the user flagged in V1/V2. Frame 1 (gold standard) uses independently-sized grey boxes: 770, 840, 520, 320, 380px — all different, all content-fitted. | **NEVER normalise grey box heights.** Each grey box is sized independently to its content using the formula `grey_box_h = (text_h × 1.15) + 65`. Uneven bottom edges across a row are correct and expected. See `design-system.md` Grey Box Properties and `algorithm.md` Pass 3 Step 3c. |
| Jun 2026 | Section headers too small at mega-frame scale — used h=70, size=42 per Header Hierarchy rule. At 6400px frame width, size=42 renders at ~13px effective at zoom-to-fit — barely visible. Frame 1 (gold standard, 2400px wide) uses size=64 for ALL headers and looks great. | The Header Hierarchy rule (size=64 for frame title, size=42 for section headers) was added to prevent "sections looking like separate frames." But Frame 1 proves size=64 works for section headers when the visual hierarchy comes from WIDTH (full-width title vs column-width sections) and COLOR TIER, not font size. | **ALL headers use h=82, size=64.** Differentiate frame title from section headers via width (full-width vs column-width) and color tier (Primary, Secondary, Tertiary, Dark, Black). See `design-system.md` Header Hierarchy. |
| Jun 2026 | Body text at size=28 — below the design system's recommended 30-36 range. Agent matched Frame 3's size=28 instead of following the minimum threshold. Frame 1 (gold standard) uses size=30 consistently. | Agent copied a reference frame's sizing instead of applying the design system rules. Frame 3 happened to use size=28 which is technically above the old minimum (26) but below the recommended range. | **Body text minimum is 30.** Never use 26-28 even if a reference frame does. The design system minimum threshold has been updated from 26 to 30. See `design-system.md` Minimum Thresholds. |
| Jun 2026 | Agent "knew" the design system from having read it earlier in the conversation — produced correct board on attempt N but failed on attempt N+1 in a new chat. | **Conversation memory ≠ file reading.** Values in the design system evolve (e.g. the TEXT y formula was corrected multiple times). The agent's "memory" of the formula from a prior conversation may be stale. Only the file is canonical. | **Re-read design-system.md EVERY time, in every conversation.** Even if you wrote it 5 minutes ago. The workspace rule `miro-enforcement.mdc` enforces this with a quote-back requirement. |
| Jun 2026 | Agent built a board that was "good" structure but generic/placeholder content. Stickies said "[add content here]" instead of real initiative content. User had to redo all content manually. | Agent treated Miro board creation as a layout exercise, not a content exercise. Focused on coordinates and styling, not on what goes ON the stickies. | **Content must be specific and pre-populated from source material.** Read the workshop pack / initiative tracker / debrief BEFORE building. Every sticky, every text box, every prompt should contain REAL project content. Generic boards are useless boards. |
| Jun 2026 | Agent used `parent=alias` in a second `layout_create` call — alias wasn't recognized, items placed at board level instead of inside the frame. | DSL aliases only work within the same `layout_create` call. Cross-call references require the full Miro URL. | **Always use the full frame URL** (returned from the first call) as `parent=` in subsequent calls. Never use the alias outside its originating call. See `algorithm.md` Pass 5 and `SKILL.md` Limitations. |
| Jun 2026 (SUCCESS) | **Board succeeded** — 102 items, 3 batches, all verified. Workshop board for a requirements breakdown session. | Agent followed the full process: read all 4 files → got DSL spec → wrote plan file → used frame URL for cross-call parent → ran verification → content was real (from workshop pack written earlier). | **The successful pattern:** (1) Read SKILL.md → algorithm.md → design-system.md → template. (2) Call `layout_get_dsl`. (3) Write coordinate plan to file. (4) Build in batches of 30-44 items. (5) Use full frame URL as `parent=` in batches 2+. (6) Run `layout_read` verification. (7) Pre-populate with REAL content from source material. |
